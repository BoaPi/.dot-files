[mgr]
ratio = [0, 1, 4]
show_hidden = true

[opener]
edit = [
	{ run = "${EDITOR:-vi} %s", desc = "NeoVim", for = "unix", block = true },
	{ run = 'code-insiders "$@"', desc = "VSCode", for = "unix", block = false },
]
